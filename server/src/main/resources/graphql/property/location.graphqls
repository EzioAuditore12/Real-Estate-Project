type Location {
  id: ID!
  address: String
  city: String
  state: String
  country: String
  postalCode: String
  latitude: Float
  longitude: Float
  coordinates: Point
}

extend type Query {
  getLocation(id: ID!): Location
}
