type Manager {
  id: ID!
  name: String
  email: String
  avatar: String
  createdAt: LocalDateTime
  managedProperties: [Property]
}

extend type Query {
  getManager(id: ID!): Manager
  getAuthenticatedManager: Manager
}
