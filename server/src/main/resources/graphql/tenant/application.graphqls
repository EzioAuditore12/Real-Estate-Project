enum ApplicationStatus {
  PENDING
  DENIED
  APPROVED
}

type Application {
  id: ID!
  startDate: LocalDateTime
  status: ApplicationStatus
  tenant: Tenant
  lease: Lease
  propertyTenantPaymentApplication: PropertyTenantPaymentApplication
}

extend type Query {
  getApplication(id: ID!): Application
}
